{
  "name": "budget-calculator",
  "version": "1.0.0",
  "description": "Simple budget calculator with Cypress tests",
  "scripts": {
    "start": "http-server . -p 8080",
    "test": "cypress open",
    "test:headless": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.6.0",
    "http-server": "^14.1.1"
  }
}
